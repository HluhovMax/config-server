#spring:
#  data:
#    mongodb:
#      host: localhost
#      port: 27017
#      username: oauth-user
#      password: password
#      database: oauth-db
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oauth_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /uaa
  port: 9091